import fetch from "node-fetch";

async function run() {
  const customerId = "5541508335";
  const loginCustomerId = "5541508335";
  const devToken = "BIfBRRk1-7UlB-YyxY-9GQ"; // from .env
  
  // We need an access token.
  // Actually, I can't easily get the access token without running the app.
}
